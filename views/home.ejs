<%- include('partials/header'); %>
<h1>HOME</h1>
<p><%= data %></p>
<% for (var i=0;i<posts.length;i++){ %>
   <h2><%= posts[i].title %></h2> 
   <p><%= posts[i].content.substring(1,205)%> ...<a href="/contents/<%= posts[i]._id %>">read more</a></p>
    <% } %>
</div>
</body>

<%- include('partials/footer'); %>